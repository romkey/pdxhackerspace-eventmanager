<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= full_page_title %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="color-scheme" content="light dark">
    <meta name="description" content="<%= meta_description %>">
    <meta name="robots" content="index, follow">
    
    <!-- Canonical URL -->
    <link rel="canonical" href="<%= meta_canonical_url %>">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="<%= meta_og_type %>">
    <meta property="og:url" content="<%= meta_canonical_url %>">
    <meta property="og:title" content="<%= full_page_title %>">
    <meta property="og:description" content="<%= meta_description %>">
    <meta property="og:site_name" content="<%= @site_config&.organization_name || 'EventManager' %>">
    <% if meta_og_image.present? %>
      <meta property="og:image" content="<%= meta_og_image %>">
    <% end %>
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= full_page_title %>">
    <meta name="twitter:description" content="<%= meta_description %>">
    <% if meta_og_image.present? %>
      <meta name="twitter:image" content="<%= meta_og_image %>">
    <% end %>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <% if @site_config&.favicon&.attached? %>
      <%= favicon_link_tag url_for(@site_config.favicon) %>
    <% end %>
    
    <!-- RSS/Atom Feeds -->
    <%= auto_discovery_link_tag :atom, events_path(format: :json), title: "Events JSON Feed" %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    
    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json"><%= organization_json_ld %></script>
    <%= content_for(:json_ld) %>
  </head>

  <body>
    <% if @site_config&.banner_image&.attached? %>
      <div class="w-100">
        <%= image_tag @site_config.banner_image, class: "img-fluid w-100", alt: "#{@site_config&.organization_name} Banner", style: "max-height: 300px; object-fit: cover;" %>
      </div>
    <% end %>

    <nav class="navbar navbar-expand-lg bg-primary navbar-dark">
      <div class="container-fluid">
        <% if @site_config&.website_url.present? %>
          <%= link_to (@site_config.organization_name || "EventManager"), @site_config.website_url, class: "navbar-brand" %>
        <% else %>
          <span class="navbar-brand mb-0"><%= @site_config&.organization_name || "EventManager" %></span>
        <% end %>
        <%= link_to root_path, class: "navbar-brand" do %>
          <i class="bi bi-house-door-fill"></i>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto">
            <li class="nav-item">
              <%= link_to "Events", events_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Calendar", calendar_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Location", location_info_path, class: "nav-link" %>
            </li>
            <% if can_create_events? %>
              <li class="nav-item">
                <%= link_to "New Event", new_event_path, class: "nav-link" %>
              </li>
            <% end %>
            <% if user_signed_in? %>
              <% if current_user.admin? %>
                <li class="nav-item">
                  <%= link_to "Users", users_path, class: "nav-link" %>
                </li>
                <li class="nav-item">
                  <%= link_to "Site Config", edit_site_config_path, class: "nav-link" %>
                </li>
              <% end %>
            <% end %>
          </ul>
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown">
                  <%= current_user.name || current_user.email %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end">
                  <li><%= link_to "My Profile", user_path(current_user), class: "dropdown-item" %></li>
                  <li><%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container mt-4">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <% if flash[:conflict] %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert" style="background-color: #dc3545; color: white; border-color: #dc3545;">
          <h5 class="alert-heading"><i class="bi bi-exclamation-triangle-fill"></i> Scheduling Conflict Warning</h5>
          <%= flash[:conflict].html_safe %>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert"></button>
        </div>
      <% end %>

      <%= yield %>
    </div>

    <footer class="mt-5 py-3 bg-light">
      <div class="container text-center text-muted">
        <small><%= @site_config&.footer_text.presence || "© #{Time.current.year} EventManager - Hackerspace Event Management System" %></small>
        <br>
        <small>
          <i class="bi bi-calendar-plus"></i> <%= link_to "Calendar Feed (.ics)", calendar_ical_path, class: "text-decoration-none" %>
          |
          <i class="bi bi-filetype-json"></i> <%= link_to "Events (JSON)", events_path(format: :json), class: "text-decoration-none" %>
          |
          <i class="bi bi-filetype-json"></i> <%= link_to "Calendar (JSON)", calendar_path(format: :json), class: "text-decoration-none" %>
        </small>
        <% if current_user&.admin? %>
          <br>
          <small class="text-muted">
            <% version = File.read(Rails.root.join('VERSION')).strip rescue 'unknown' %>
            <i class="bi bi-tag"></i> <%= version %> 
            | 
            <i class="bi bi-server"></i> <%= Rails.env.titleize %>
          </small>
        <% end %>
      </div>
    </footer>
  </body>
</html>
