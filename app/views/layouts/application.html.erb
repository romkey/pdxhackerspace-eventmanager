<!DOCTYPE html>
<html lang="en">
  <head>
    <title><%= full_page_title %></title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="color-scheme" content="light dark">
    <meta name="description" content="<%= meta_description %>">
    <meta name="robots" content="index, follow">
    
    <!-- Canonical URL -->
    <link rel="canonical" href="<%= meta_canonical_url %>">
    
    <!-- Open Graph / Facebook -->
    <meta property="og:type" content="<%= meta_og_type %>">
    <meta property="og:url" content="<%= meta_canonical_url %>">
    <meta property="og:title" content="<%= full_page_title %>">
    <meta property="og:description" content="<%= meta_description %>">
    <meta property="og:site_name" content="<%= @site_config&.organization_name || 'EventManager' %>">
    <% if meta_og_image.present? %>
      <meta property="og:image" content="<%= meta_og_image %>">
    <% end %>
    
    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= full_page_title %>">
    <meta name="twitter:description" content="<%= meta_description %>">
    <% if meta_og_image.present? %>
      <meta name="twitter:image" content="<%= meta_og_image %>">
    <% end %>
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <% if @site_config&.favicon&.attached? %>
      <%= favicon_link_tag url_for(@site_config.favicon) %>
    <% end %>
    
    <!-- RSS/Atom Feeds -->
    <%= auto_discovery_link_tag :rss, events_rss_path, title: "#{@site_config&.organization_name || 'Events'} RSS Feed" %>
    <%= auto_discovery_link_tag :atom, events_path(format: :json), title: "Events JSON Feed" %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>
    
    <!-- JSON-LD Structured Data -->
    <script type="application/ld+json"><%= organization_json_ld %></script>
    <%= content_for(:json_ld) %>
    
    <!-- Additional head content from views -->
    <%= content_for(:head) %>
    
    <!-- Matomo Analytics -->
    <% if @site_config&.matomo_enabled && @site_config&.matomo_url.present? && @site_config&.matomo_site_id.present? %>
      <script>
        var _paq = window._paq = window._paq || [];
        /* tracker methods like "setCustomDimension" should be called before "trackPageView" */
        _paq.push(['trackPageView']);
        _paq.push(['enableLinkTracking']);
        (function() {
          var u="<%= @site_config.matomo_url %>/";
          _paq.push(['setTrackerUrl', u+'matomo.php']);
          _paq.push(['setSiteId', '<%= @site_config.matomo_site_id %>']);
          var d=document, g=d.createElement('script'), s=d.getElementsByTagName('script')[0];
          g.async=true; g.src=u+'matomo.js'; s.parentNode.insertBefore(g,s);
        })();
      </script>
      <noscript><p><img referrerpolicy="no-referrer-when-downgrade" src="<%= @site_config.matomo_url %>/matomo.php?idsite=<%= @site_config.matomo_site_id %>&amp;rec=1" style="border:0;" alt="" /></p></noscript>
    <% end %>
  </head>

  <body>
    <a href="#main-content" class="visually-hidden-focusable skip-link">Skip to main content</a>
    <header>
      <% if @site_config&.banner_image&.attached? %>
        <div class="w-100">
          <%= image_tag @site_config.banner_image, class: "img-fluid w-100", alt: "#{@site_config&.organization_name} Banner", style: "max-height: 300px; object-fit: cover;", loading: "eager" %>
        </div>
      <% end %>

      <nav class="navbar navbar-expand-lg bg-primary navbar-dark" aria-label="Main navigation">
      <div class="container-fluid">
        <% if @site_config&.website_url.present? %>
          <%= link_to (@site_config.organization_name || "EventManager"), @site_config.website_url, class: "navbar-brand" %>
        <% else %>
          <span class="navbar-brand mb-0"><%= @site_config&.organization_name || "EventManager" %></span>
        <% end %>
        <%= link_to root_path, class: "navbar-brand", "aria-label": "Home" do %>
          <i class="bi bi-house-door-fill" aria-hidden="true"></i>
          <span class="visually-hidden">Home</span>
        <% end %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav me-auto nav-chips">
            <% if user_signed_in? %>
              <li class="nav-item">
                <%= link_to dashboard_path, class: "nav-link" do %>
                  <i class="bi bi-speedometer2" aria-hidden="true"></i> Dashboard
                <% end %>
              </li>
            <% end %>
            <li class="nav-item">
              <%= link_to "Events", events_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Calendar", calendar_path, class: "nav-link" %>
            </li>
            <li class="nav-item">
              <%= link_to "Location", location_info_path, class: "nav-link" %>
            </li>
            <% if can_create_events? %>
              <li class="nav-item">
                <%= link_to "New Event", new_event_path, class: "nav-link" %>
              </li>
            <% end %>
            <% if user_signed_in? && current_user.admin? %>
              <li class="nav-item admin-separator"></li>
              <li class="nav-item">
                <%= link_to "Users", users_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Reminder Postings", reminder_postings_path, class: "nav-link" %>
              </li>
              <li class="nav-item">
                <%= link_to "Site Config", edit_site_config_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
          <ul class="navbar-nav">
            <% if user_signed_in? %>
              <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-bs-toggle="dropdown" aria-expanded="false" aria-haspopup="true">
                  <%= current_user.name || current_user.email %>
                </a>
                <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="navbarDropdown">
                  <li><%= link_to "My Profile", user_path(current_user), class: "dropdown-item" %></li>
                  <li><%= link_to "Edit Profile", edit_user_registration_path, class: "dropdown-item" %></li>
                  <li><hr class="dropdown-divider"></li>
                  <li><%= button_to "Sign Out", destroy_user_session_path, method: :delete, class: "dropdown-item" %></li>
                </ul>
              </li>
            <% else %>
              <li class="nav-item">
                <%= link_to "Sign In", new_user_session_path, class: "nav-link" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>
    </header>

    <main id="main-content" class="container mt-4" role="main">
      <% if notice %>
        <div class="alert alert-success alert-dismissible fade show" role="alert">
          <%= notice %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close notification"></button>
        </div>
      <% end %>

      <% if alert %>
        <div class="alert alert-danger alert-dismissible fade show" role="alert" aria-live="assertive">
          <%= alert %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close alert"></button>
        </div>
      <% end %>

      <% if flash[:conflict] %>
        <div class="alert alert-warning alert-dismissible fade show" role="alert" aria-live="assertive" style="background-color: #dc3545; color: white; border-color: #dc3545;">
          <h2 class="alert-heading h5"><i class="bi bi-exclamation-triangle-fill" aria-hidden="true"></i> Scheduling Conflict Warning</h2>
          <%= flash[:conflict].html_safe %>
          <button type="button" class="btn-close btn-close-white" data-bs-dismiss="alert" aria-label="Close warning"></button>
        </div>
      <% end %>

      <%= yield %>
    </main>

    <footer class="mt-5 py-3 bg-light" role="contentinfo">
      <div class="container text-center text-muted">
        <small><%= @site_config&.footer_text.presence || "© #{Time.current.year} EventManager - Hackerspace Event Management System" %></small>
        <br>
        <nav aria-label="Feed links">
          <small>
            <i class="bi bi-rss" aria-hidden="true"></i> <%= link_to "RSS Feed", events_rss_path, class: "text-decoration-none" %>
            <span aria-hidden="true">|</span>
            <i class="bi bi-calendar-plus" aria-hidden="true"></i> <%= link_to "Calendar (.ics)", calendar_ical_path, class: "text-decoration-none" %>
            <span aria-hidden="true">|</span>
            <i class="bi bi-filetype-json" aria-hidden="true"></i> <%= link_to "JSON", events_path(format: :json), class: "text-decoration-none" %>
            <span aria-hidden="true">|</span>
            <i class="bi bi-github" aria-hidden="true"></i> <%= link_to "GitHub", "https://github.com/romkey/pdxhackerspace-eventmanager", target: "_blank", rel: "noopener", class: "text-decoration-none" %>
          </small>
        </nav>
        <% if current_user&.admin? %>
          <br>
          <small class="text-muted">
            <% version = File.read(Rails.root.join('VERSION')).strip rescue 'unknown' %>
            <i class="bi bi-tag"></i> <%= version %> 
            | 
            <i class="bi bi-server"></i> <%= Rails.env.titleize %>
          </small>
        <% end %>
      </div>
    </footer>
  </body>
</html>
